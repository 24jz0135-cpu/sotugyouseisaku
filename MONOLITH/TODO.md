# TODO（タブを下部固定）

- [x] 現状確認：タブ（記録/分析/画像）は MONOLITH/app/App.tsx の `nav`（上部）に実装されている
- [x] `MONOLITH/app/App.tsx` を編集：`nav` を画面下固定（absolute bottom）に変更
- [x] `MONOLITH/app/App.tsx` を編集：固定タブの高さ分だけ `content` に `paddingBottom` を追加

- [ ] 起動してUI確認（ボタンが下、本文が隠れない）

